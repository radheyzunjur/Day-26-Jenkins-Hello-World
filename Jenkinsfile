pipeline {
 agent any
  stages {
     stage('Hello') {
         steps {
              node {
               echo 'Hello World Using Jenkins File'
                   }
               }
                 }
         }
}
